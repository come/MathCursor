# Release — 0.3.0

**Date :** 2026-04-24
**Kind :** Release
**Statut :** acté

## Décision

Bump mineur 0.2.0 → 0.3.0, nouvel installer pour les beta-testeurs.

## Contenu de la release (depuis 0.2.0)

- **Feat** : Feedback in-popup "Signaler une erreur" (dialog modal, envoi
  pluggable clipboard/HTTP).
- **Feat** : Matching préfixe — propositions partielles avec `\ldots` en rouge
  quand aucun pattern complet ne matche.
- **Feat** : Patterns `sum`/`product`/`integral` séparateur espace (comme la
  limite) + angle `\widehat{ABC}`.
- **Fix** : Span Ctrl+Espace respecte les brackets/parens (`;`/`,` internes
  aux intervalles ne coupent plus la span).

## Conséquences

- `AssemblyInfo.cs` : `0.3.0.0`
- `installer/MathCursor.iss` : `0.3.0`
- Popup "Aide" du ruban : `Version 0.3.0 — beta`
- Installer : `MathCursor-Setup-0.3.0.exe`

## Validé par l'utilisateur

> "ok pour l'organisation et le suivi du projet [...] et on y va"

## Statut

acté, installer buildé (`adapter-vsto/installer/output/MathCursor-Setup-0.3.0.exe`).
