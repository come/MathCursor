# Artefacts de build de l'extension (régénérés par build_oxt.py).
MathCursor.oxt
build/

# Spikes jetables (NER) + dépendances natives vendored.
_spike_*
_vendor/
vendor/

# Coquille popup : source = rust/mc-popup ; les binaires shell/<tag>/ sont
# buildés/stagés par build_oxt.py (plus de source dupliquée ici).
shell/
