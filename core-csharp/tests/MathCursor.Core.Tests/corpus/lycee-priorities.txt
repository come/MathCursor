# =============================================================================
# Corpus de tests — extensions demandées (50 catégories lycée→prépa)
# Format : input => latex_attendu
# Les lignes commençant par # sont des commentaires / titres de catégorie
# Chargé par CorpusTests qui renvoie un rapport pass/fail par catégorie.
# =============================================================================


# -----------------------------------------------------------------------------
# 1. LIMITES À DROITE / À GAUCHE (c+/c- : "c" comme cible, pas "a" = stopword)
# -----------------------------------------------------------------------------
lim x->0+ f(x) => \lim_{x \to 0^+} f(x)
lim x->0- f(x) => \lim_{x \to 0^-} f(x)
lim x -> 0 + => \lim_{x \to 0^+}
lim x -> 0+ 1/x => \lim_{x \to 0^+} \frac{1}{x}


# -----------------------------------------------------------------------------
# 2. LIMITES SUP / INF
# -----------------------------------------------------------------------------
limsup x->inf f(x) => \limsup_{x \to \infty} f(x)
liminf n->inf u_n => \liminf_{n \to \infty} u_n


# -----------------------------------------------------------------------------
# 3. DÉRIVÉES PARTIELLES
# -----------------------------------------------------------------------------
df/dx => \frac{df}{dx}
partial f / partial x => \frac{\partial f}{\partial x}
nabla f => \nabla f


# -----------------------------------------------------------------------------
# 4. DÉRIVÉES D'ORDRE SUPÉRIEUR
# -----------------------------------------------------------------------------
f''(x) => f''(x)
f'''(x) => f'''(x)
y'' + 2y' + y = 0 => y'' + 2y' + y = 0


# -----------------------------------------------------------------------------
# 5. INTÉGRALES AVANCÉES (séparateur espace entre dx et dy)
# -----------------------------------------------------------------------------
iint f dx dy => \iint f \, dx \, dy
oint f dl => \oint f \, dl


# -----------------------------------------------------------------------------
# 6. NOTATIONS DE LANDAU (o / O / Theta)
# -----------------------------------------------------------------------------
o(x^2) => o(x^2)
O(n log n) => O(n \log n)
Theta(n) => \Theta(n)
Omega(n) => \Omega(n)


# -----------------------------------------------------------------------------
# 7. ÉQUIVALENCE ASYMPTOTIQUE
# -----------------------------------------------------------------------------
f ~ g => f \sim g


# -----------------------------------------------------------------------------
# 8. POINT DE NEWTON (DÉRIVÉE TEMPORELLE)
# -----------------------------------------------------------------------------
dot x => \dot{x}
ddot x => \ddot{x}


# -----------------------------------------------------------------------------
# 9. MATRICES (mat(1,2;3,4) → pmatrix : TODO, pattern dédié à écrire)
# -----------------------------------------------------------------------------
I_n => I_n
diag(1,2,3) => \operatorname{diag}(1,2,3)


# -----------------------------------------------------------------------------
# 10. TRANSPOSÉE, INVERSE, CONJUGUÉE
# -----------------------------------------------------------------------------
A^T => A^T
A^-1 => A^{-1}
A^* => A^*


# -----------------------------------------------------------------------------
# 11. DÉTERMINANT, TRACE, RANG
# -----------------------------------------------------------------------------
det(A) => \det(A)
tr(A) => \operatorname{tr}(A)
rg(A) => \operatorname{rg}(A)


# -----------------------------------------------------------------------------
# 12. PRODUITS MATRICIELS / TENSORIELS
# -----------------------------------------------------------------------------
A otimes B => A \otimes B
A oplus B => A \oplus B


# -----------------------------------------------------------------------------
# 13. ESPACES VECTORIELS
# -----------------------------------------------------------------------------
Vect(u,v) => \operatorname{Vect}(u,v)
Ker(f) => \ker(f)
dim(E) => \dim(E)


# -----------------------------------------------------------------------------
# 15. CONNECTEURS LOGIQUES (p/q minuscules pour éviter canonicalisation Q→ℚ)
# -----------------------------------------------------------------------------
p ==> q => p \Rightarrow q
p <=> q => p \iff q


# -----------------------------------------------------------------------------
# 16. ENSEMBLES : DIFFÉRENCE, COMPLÉMENTAIRE
# -----------------------------------------------------------------------------
E \ F => E \setminus F


# -----------------------------------------------------------------------------
# 17. INCLUSION
# -----------------------------------------------------------------------------
A subset B => A \subset B
A subseteq B => A \subseteq B


# -----------------------------------------------------------------------------
# 19. CARDINALITÉ
# -----------------------------------------------------------------------------
card(A) => \operatorname{card}(A)


# -----------------------------------------------------------------------------
# 20. LOIS DE PROBABILITÉ
# -----------------------------------------------------------------------------


# -----------------------------------------------------------------------------
# 21. ÉCART-TYPE, COVARIANCE
# -----------------------------------------------------------------------------
cov(X,Y) => \operatorname{Cov}(X, Y)


# -----------------------------------------------------------------------------
# 22. INDICATEUR
# -----------------------------------------------------------------------------


# -----------------------------------------------------------------------------
# 23. FONCTIONS HYPERBOLIQUES
# -----------------------------------------------------------------------------
sh(x) => \sinh(x)
ch(x) => \cosh(x)
th(x) => \tanh(x)
sinh(x) => \sinh(x)
cosh(x) => \cosh(x)
tanh(x) => \tanh(x)
argsh(x) => \operatorname{argsh}(x)
argch(x) => \operatorname{argch}(x)
argth(x) => \operatorname{argth}(x)


# -----------------------------------------------------------------------------
# 24. TRIGONOMÉTRIE ÉTENDUE
# -----------------------------------------------------------------------------
cot(x) => \cot(x)
cotan(x) => \cot(x)
sec(x) => \sec(x)
csc(x) => \csc(x)


# -----------------------------------------------------------------------------
# 25. PUISSANCES DE FONCTIONS TRIGO
# -----------------------------------------------------------------------------
sin^2(x) => \sin^2(x)
sin^2 x => \sin^2(x)
cos^3 x => \cos^3(x)
tan^2 x => \tan^2(x)
sh^2(x) => \sinh^2(x)


# -----------------------------------------------------------------------------
# 26. PGCD, PPCM
# (évite "a" comme variable : c'est un connecteur FR "de 0 à 1")
# -----------------------------------------------------------------------------
pgcd(m,n) => \gcd(m,n)
gcd(m,n) => \gcd(m,n)
ppcm(m,n) => \operatorname{ppcm}(m,n)


# -----------------------------------------------------------------------------
# 27. DIVISIBILITÉ
# -----------------------------------------------------------------------------


# -----------------------------------------------------------------------------
# 28. CONGRUENCE / MODULO (m/n pour éviter "a" = stopword FR)
# -----------------------------------------------------------------------------
m mod n => m \bmod n


# -----------------------------------------------------------------------------
# 29. SUITES INDEXÉES
# -----------------------------------------------------------------------------


# -----------------------------------------------------------------------------
# 30. NOTATIONS CHIMIQUES (nécessite tokenizer adapté)
# -----------------------------------------------------------------------------


# -----------------------------------------------------------------------------
# 31. APPROXIMATION, PROPORTIONNALITÉ
# -----------------------------------------------------------------------------
pi approx 3.14 => \pi \approx 3.14
y propto x => y \propto x


# -----------------------------------------------------------------------------
# 32. PLUS OU MOINS
# -----------------------------------------------------------------------------
+/- 1 => \pm 1
+- 2 => \pm 2


# -----------------------------------------------------------------------------
# 33. COMPOSITION DE FONCTIONS
# -----------------------------------------------------------------------------
f ° g => f \circ g
f ∘ g => f \circ g


# -----------------------------------------------------------------------------
# 34. RESTRICTION (non implémenté)
# -----------------------------------------------------------------------------


# -----------------------------------------------------------------------------
# 35. DOMAINE, IMAGE (partiel)
# -----------------------------------------------------------------------------
Dom(f) => \operatorname{Dom}(f)


# -----------------------------------------------------------------------------
# 40. COEFFICIENTS BINOMIAUX
# -----------------------------------------------------------------------------
C(n,k) => \binom{n}{k}
C(5,2) => \binom{5}{2}
C_n^k => \binom{n}{k}
binom(n,k) => \binom{n}{k}
comb(n,k) => \binom{n}{k}


# -----------------------------------------------------------------------------
# 41. FACTORIELLE
# -----------------------------------------------------------------------------
n! => n!
5! => 5!
(n+1)! => (n+1)!


# =============================================================================
# FIN
# =============================================================================
