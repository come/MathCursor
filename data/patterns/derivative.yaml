# Pattern derivative — dérivée d'une expression
# 2 slots positionnels : var, expression (multi-token)
# Migré depuis DerivativeTemplate.cs en P9e (2026-05-21)

template_id: derivative
order: 0

heads:
  - { source: Derive,  latex: '\frac{d}{d}', mutation: derive, weight: 100 }
  - { source: derive,  latex: '\frac{d}{d}', mutation: derive, weight: 95 }
  - { source: dérivée, latex: '\frac{d}{d}', mutation: derive, weight: 90 }
  - { source: dérive,  latex: '\frac{d}{d}', mutation: derive, weight: 85 }

slots:
  - { position: 0, name: var }
  - { position: 1, name: expression, multi_token: true }

scoring:
  base: 33
  per_slot: 33

render:
  preview: '\frac{d}{d<var>} <expression>'
  hint:    '\frac{d}{d<var|\square>} <expression|\square>'
  description: 'd/d<var|▭> <expression|▭>'
